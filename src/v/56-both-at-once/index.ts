// PROTOTYPE round 3 — Both at once. Same kit as the other two; only the hero and section order change.
import { page } from '../r3-kit/kit';

export function render(root: HTMLElement) { page(root, 'both'); }
