export const meta = { name: 'Comic strip', angle: "Problem first, then both at once: a four-panel strip opens on the reader's own mess (three code-review folders, 'I'll try it later') so they laugh in recognition, then the last panel shows both fixes together; two big panels follow, the folders first because that pain is universal, the 'later' pile second.", focus: 'both' } as const;
